# vrp-canonical-spec
Canonical specification of VRP (Veil Routing Protocol): a continuity-first execution model where session identity is independent of transport and correctness is enforced at the commit layer.
